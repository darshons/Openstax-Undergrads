# Core API dependencies (pinned to the versions validated on Ubuntu / Python 3.12)
fastapi[standard]==0.141.1
python-dotenv==0.21.1
anthropic==0.120.2
google-genai==2.10.0
markdownify==1.2.3
requests==2.34.2
beautifulsoup4==4.15.0
lxml==6.1.1
rapidfuzz==3.14.5
pillow==10.4.0
moviepy
imageio-ffmpeg
openai-whisper

# openai-whisper pulls in torch (multi-GB download). Required by
# solo_clip/transcribe.py to trim each raw clip to its actual speaking span
# before stitching - not just an optional Transcript_Eval_Pipeline extra
# anymore.

# Manim video generation has its own pinned requirements:
#   pip install -r Video_Generation_Pipeline/manim_generator/requirements.txt
supabase
